name: app_theme
description: "공용 다크/라이트 테마 + 폼 컴포넌트 (accent는 앱별 주입)."
publish_to: 'none'
version: 0.0.1
resolution: workspace

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  design_system: ^0.0.1

dev_dependencies:
  flutter_lints: ^6.0.0
