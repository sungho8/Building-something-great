/// 공용 다크/라이트 테마 + 폼 컴포넌트.
///
/// 중립 팔레트(surface/text/border)는 공유하고, accent(브랜드색)만 앱별로 주입한다.
/// design_system(흰 배경 전용)과 달리 이 패키지는 라이트/다크를 모두 지원한다.
library;

export 'src/ui_theme.dart';
export 'src/ui_widgets.dart';
