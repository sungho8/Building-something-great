/// 공용 앱 인프라 — 광고·저장·분석·공용화면.
///
/// 두 번째 앱부터 본격적으로 채운다. 지금은 자리만 잡아둔다.
library;

/// 앱 공통 메타 정보 placeholder.
class AppInfo {
  const AppInfo({required this.name, required this.version});

  final String name;
  final String version;
}
